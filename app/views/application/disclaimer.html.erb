<script>
dismissDisclaimer = function(event) {
  event.preventDefault();

  if (typeof(Storage) !== "undefined") {
    localStorage.setItem("disclaimerConfirm", "1")
  }

  window.location = '<%= inbox_url %>'
}
</script>

<center>
  <div class="panel" style="text-align: center">
    <h1>Oops... No mailboxes found. Is this your first visit?</h1>

    <p><b>Read this very carefully 🙏</b></p>
    <div style="text-align: left">
      <p>This website is just an interface to the NEO blockchain: there are no user accounts and we have no knowledge of your data.</p>
      <p>You'll need a <a href="#">NEO wallet</a> to use Phantasma. You'll be asked to add your wallet details (public address and private key) to use this service as a proxy to the Phantasma smart contract.</p>
      <p>Your wallet details will be saved in local storage with a time to live of 7 days. You can delete this information at any time or use a private browser session.</p>
    </div>

    <a href="#" onclick="dismissDisclaimer(event)" class="btn btn-primary">
      Got it
    </a>
  </div>
</center>